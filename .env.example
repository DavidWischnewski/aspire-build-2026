# Supabase — shared workshop project. These are publishable keys, safe in repo.
NEXT_PUBLIC_SUPABASE_URL=https://qdxfcouepebuajeaxueg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable__PBFkCkVkUMC-0MwgkAsXQ_8_cmed8i

# Anthropic — NOT needed for the workshop. AI calls hit the mocked /api/chat.
# Only set this if you swap the mock for real Claude post-workshop
# (see swap instructions in src/app/api/chat/route.ts).
# ANTHROPIC_API_KEY=your-api-key
